import Hero from "../Global/Hero"

const HeroCheckout = () => {
  return (
    <Hero 
        title="Checkout"
        firstPath="Home"
        secondPath="Checkout"
    />
  )
}

export default HeroCheckout